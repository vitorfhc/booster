{
  "name": "Booster",
  "version": "1.0.0",
  "description": "An API for RocketMat",
  "main": "src/index.js",
  "repository": "https://github.com/vitorfhc/booster",
  "author": "Vitor Falcão",
  "license": "GPL-3.0",
  "scripts": {
    "dev": "yarn nodemon --exec 'yarn lint && node' src/index.js",
    "lint": "yarn eslint src --ext .js",
    "prod": "yarn pm2 start src/index.js && tail -f /dev/null",
    "test": "echo Run tests"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.7.9",
    "pm2": "^4.1.2"
  },
  "devDependencies": {
    "eslint": "^6.6.0",
    "nodemon": "^1.19.4"
  }
}
